package education.knowing.repository;

import education.knowing.entity.FolderQna;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FolderQnaRepository extends JpaRepository<FolderQna, Long> {
}
